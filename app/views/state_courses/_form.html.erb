<%= form_for(@state_course) do |f| %>
  <% if @state_course.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@state_course.errors.count, "error") %> prohibited this state_course from being saved:</h2>

      <ul>
      <% @state_course.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
