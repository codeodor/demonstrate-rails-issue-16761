<%= form_for(@district_course) do |f| %>
  <% if @district_course.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@district_course.errors.count, "error") %> prohibited this district_course from being saved:</h2>

      <ul>
      <% @district_course.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :subject_id %><br>
    <%= f.number_field :subject_id %>
  </div>
  <div class="field">
    <%= f.label :district_id %><br>
    <%= f.number_field :district_id %>
  </div>
  <div class="field">
    <%= f.label :state_course_id %><br>
    <%= f.number_field :state_course_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
